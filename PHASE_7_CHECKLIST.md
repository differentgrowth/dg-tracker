# Phase 7 — Internal Report v1: Implementation Checklist

Temporary working file. Tracks the Phase 7 plan from `MVP_ROADMAP.md`. Delete on phase completion.

## Goal

Create an internal-only monthly report workflow that lets a signed-in team member generate a report for a client, persist the result, and read it inside the app.

## Locked decisions

- [x] **Report audience:** internal users only; no public sharing or client portal in MVP.
- [x] **Delivery:** in-app read-only report page first; PDF and email delivery are deferred.
- [x] **Period:** monthly reporting is the default, with explicit `periodStart` and `periodEnd` stored on `Report`.
- [x] **Data source:** report summaries are derived from stored `RankingSnapshot` and GSC performance snapshot data, not live GSC calls.
- [x] **Ranking semantics:** GSC `avgPosition` remains average position; do not present it as exact rank.
- [x] **Summary storage:** store the v1 generated summary as JSON on `Report` and refresh by unique client/period.

## 1. Schema & migration

- [x] Store generated summary JSON on `Report` for the internal v1; defer normalized report sections until reporting needs stabilize.
- [x] Add report summary fields needed for Phase 7, such as top wins, top losses, traffic deltas, keyword counts, and narrative notes.
- [x] Add a report status for generated reports.
- [x] Add and commit migration `20260512190000_add_report_summary`.
- [x] Run `pnpm prisma generate`.

## 2. Validators & actions

- [x] Add `lib/validators/report.ts` for client ID and reporting period validation.
- [x] Add a Server Action to generate a report for a client and period.
- [x] Refresh/regenerate an existing report if the user reruns the same period.
- [x] Revalidate client report list and report detail pages after generation.

## 3. Report generation service

- [x] Expand `lib/services/report.service.ts` with `generateClientReport(opts)`.
- [x] Load the client, active keywords, ranking snapshots, and performance snapshots for the requested period.
- [x] Compare against the previous equivalent period for deltas where data exists.
- [x] Calculate top wins and top losses from average position changes.
- [x] Calculate traffic deltas from clicks, impressions, CTR, and average position.
- [x] Include clear empty states in the generated summary when insufficient comparison data exists.
- [x] Persist the generated summary and return counts/IDs only; do not return raw GSC rows.

## 4. Reports UI

- [x] Add a report generation form on the client reports surface.
- [x] Add the read-only report detail route at `app/(dashboard)/clients/[clientId]/reports/[reportId]/page.tsx`.
- [x] Render report sections for executive summary, KPI changes, top wins, top losses, and keyword opportunities.
- [x] Link generated reports from the client page and reports list.
- [x] Show report metadata: period, generated date, generated by, and data completeness.

## 5. Authorization & safety

- [x] Require an authenticated session for all report pages and actions.
- [x] Ensure users can only generate reports for existing non-archived clients.
- [x] Avoid logging raw client data, GSC rows, or sensitive report contents.
- [x] Surface safe, actionable errors at action boundaries.

## 6. Tests

- [x] Add service tests for top wins/losses, traffic deltas, and no-comparison-data behavior.
- [ ] Add action validation tests if the project adds an action test harness.
- [x] Keep route/component coverage manual for now because the UI is server-rendered and logic stays in the service.

## 7. Verification

- [x] `pnpm typecheck` clean.
- [x] `pnpm test` clean.
- [ ] `pnpm build` clean. Current environment cannot fetch Google Fonts during `next/font` build.
- [x] `npx ultracite check` clean.
- [ ] Manual smoke test: generate last month's report for a client with snapshots, open the report page, refresh/regenerate, and confirm no duplicate report rows unless intentionally versioned. Requires a running app/database with seeded or synced snapshots.

## Commit sequence

1. [x] `feat(reports): add report summary schema`
2. [x] `feat(reports): generate monthly client summaries`
3. [x] `feat(reports): add internal report pages`
4. [x] `test(reports): cover report summary calculations`
